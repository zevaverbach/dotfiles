# Screen Recording to GIF

Record a portion of the screen (or the full screen) to a GIF using ffmpeg.

Run the `gif` CLI tool at `~/.local/bin/gif` with the user's arguments:

```bash
gif $ARGUMENTS
```

Report the output to the user.
